{% extends "base.html.twig" %}

{% block title %}Editer une recette{% endblock %}

{% block body %}
    <h1  class="text text-primary text-center text-uppercase ">{{ recipe.title }}</h1>

    {{ form_start(form)}}
        <div class="row">
            <div class="col-sm-6">
                {{ form_row(form.title) }}
            </div>
            <div class="col-sm-6">
                {{ form_row(form.slug) }}
            </div>
        </div>
    {{ form_end(form) }}
{% endblock body %}